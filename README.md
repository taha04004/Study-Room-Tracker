1. Install python

2. How to setup the virtual enviornment
Windows
python -m venv venv
venv\Scripts\activate

macOS/Linux
python3 -m venv venv
source venv/bin/activate

after activation your terminal should show (venv) in front of the file lcoation/folder path

3. Install all required packages
pip install -r requirements.txt
pip install flask werkzeug

4. Run the application
python app.py

or just hit run

5. Using the system
Ctrl + click on
http://127.0.0.1:5000
and it will open in your browser

7. Staff login
Username: admin
Password: Admin123 
